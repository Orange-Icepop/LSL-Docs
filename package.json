{
  "devDependencies": {
    "vitepress": "^1.5.0"
  },
  "scripts": {
    "docs:dev": "vitepress dev LSL --host 0.0.0.0 --port 25405",
    "docs:build": "vitepress build LSL --host 0.0.0.0 --port 25405",
    "docs:preview": "vitepress preview LSL --host 0.0.0.0 --port 25405"
  }
}
